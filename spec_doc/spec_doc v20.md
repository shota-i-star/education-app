# AZism 教育進捗管理 — プロジェクト仕様書

> この仕様書は、AIとの会話が長くなっても開発の方向性を維持するための「共通の地図」です。
> 新しい会話を始めるときは、この仕様書を貼り付けてから質問してください。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | AZism 教育進捗管理 |
| 種類 | Webアプリ（ブラウザで動作）・PWA対応（ホーム画面追加可） |
| 対象ユーザー | 全社員（MG・メンバー・管理者） |
| 開発者のスキル | プログラミング未経験。作りながら学ぶスタイルで進める |
| 公開方法 | GitHub Pages（静的ホスティング） |

---

## 2. 解決したい課題

- 誰がどの教育項目をどこまで終えたか、一目でわからない
- テストの点数や進捗を手作業（Excel等）で管理しており、集計が煩雑
- 上司とメンバーで見せる情報を分けたい

---

## 3. 主な機能要件

### メンバー向け画面
- チェックリスト形式で教育項目を完了マークできる
- テストを受けて点数が記録される（**カテゴリ×等級ごと**にテストが設定されている場合のみボタン表示）
- **テストボタンを押すと問題集一覧モーダルが表示され、受けるテストを選択できる（v20〜）**
- **受験済みテストは結果閲覧のみ可能（回答編集不可・v20〜）**
- **採点待ちのテストは「採点待ち」ステータスで表示され再受験不可（v20〜）**
- 過去の等級のコンテンツを閲覧できる
- チェック時に画面が白くフラッシュしない（silent再描画）
- 進捗を「全体進捗率」と「現在の等級の進捗率」の2軸で表示
- 進捗バーはヘッダー直下に貼り付き（sticky）、スクロールしても常時表示される
- カテゴリは管理画面の sort_order 順で表示される
- カテゴリヘッダーはスクロールすると貼り付き、次のカテゴリに到達すると自動で切り替わる
- カテゴリごとにトグル開閉が可能。▼/▶で開け閉めでき、チェック後の再描画でも開閉状態が保持される
- 等級タブはタブの変更で等級別進捗のみ切り替わり、全体進捗は固定表示

### テスト画面
- **問題集IDを URL パラメータ（?set_id=N）で受け取り、DB からテスト内容を動的に読み込む（v20〜）**
- **問題タイプ：4択（choice）/ ○×（truefalse）/ 記述（text）の3種類に対応（v20〜）**
- **合否判定：選択式の自動採点点数＋記述式の採点後点数の合計 ≥ 合格基準点 で合格（v20〜）**
- **記述式がある場合は提出後「採点中」ステータスとなり、採点完了まで合否確定しない（v20〜）**
- 受験済みのテストは過去の回答・結果を表示（回答編集不可）
- 採点中・受験済みの場合は再受験不可（二重送信ガード付き）

### MG・管理者向け画面
- 全メンバーの進捗を一覧・集計して見られる
- 進捗率は「全体進捗率」「現等級進捗率」の2軸で表示
- サマリーカードに「メンバー数」「平均進捗率（全体）」「平均進捗率（現等級）」「テスト平均点」「未合格者数」の5指標を表示
- 進捗率セクションは「エリア別」「店舗別」「等級別」「個人別」の4タブ
- 等級別タブ：0〜3等級ごとにメンバーをグループ化し、展開でメンバー一覧表示
- エリア別・店舗別タブで行をクリックするとその配下のメンバー一覧が展開表示される
- テストの平均点・個人別スコアを確認できる
- 進捗率をエリア別・店舗別グラフで把握できる
- メンバー詳細画面でチェック状況・テスト履歴・コメントを確認できる
- **メンバー詳細のテスト履歴：問題集名・合計点（選択式＋記述式）・採点中ステータスを表示（v20〜）**
- メンバー詳細画面からPDFで出力できる

### 管理者向け画面
- メンバー管理（追加・部署変更・等級変更・役割変更・削除）
- メンバー編集モーダル：名前・社員番号・パスワードを1画面で変更
- メンバー追加時に部署の設定が必須
- 社員番号変更・パスワードリセット（全権管理者のみ）
- メンバーデータのCSVエクスポート
- CSV一括インポート：メンバー・カテゴリ・店舗・項目
- 部署・エリア・店舗管理（追加・削除・トグル表示・店舗のエリア間移動）
- **部署エリア店舗管理の店舗移動・削除ボタンを右寄せ表示（v20〜）**
- 事業部名・エリア名の編集（全権管理者のみ）
- カテゴリ管理（追加・複製・削除・CSV一括追加・ドラッグ＆ドロップ並べ替え）
- カテゴリ編集モーダル：カテゴリ名の編集に加え、配下の項目をドラッグ＆ドロップで並び替え可能
- 項目管理（追加・No.表示・項目名編集・複製・削除・CSV一括追加）
- テスト管理（部署×等級ごとに整理・トグル表示）
- **問題集追加：まず部署を選択 → その部署に紐づくカテゴリのみ選択可能（v20〜）**
- **問題タイプ：4択・○×・記述の3種類。○×は管理画面で「○が正解」「×が正解」を設定（v20〜）**
- 記述式テストの採点（各問題の設定配点を最大点として入力）
- **採点入力欄：問題に設定された配点を最大点として表示（v20〜）**
- 再試験管理（合計点が合格基準未満かつ採点完了済みのみ対象）
- カテゴリ複製機能（manager_admin のみ）

### 共通機能
- ログイン機能（役割に応じて画面を切り替え）
- ログアウト機能（全画面共通）
- データはSupabaseに保存（リロードしても消えない）
- スマートフォン対応（member_top.html・test_page.html）
- PWA対応
- AZ-GROUPロゴを全ページヘッダーに表示
- ヘッダーの「AZism 教育進捗管理」タイトルをタップすると画面が更新される

---

## 4. 開発フェーズ（ロードマップ）

| フェーズ | テーマ | 状態 |
|---------|--------|------|
| **1** | 画面の骨格を作る | ✅ 完了 |
| **2** | データを動かす | ✅ 完了 |
| **3** | ログインで役割を分ける | ✅ 完了 |
| **4** | 集計・レポートを作る | ✅ 完了 |
| **5** | 仕上げ・公開準備 | ✅ 完了 |
| **6** | GitHub Pages で公開・運用改善 | 🔄 進行中 |

---

## 5. 技術スタック

| 役割 | 技術 |
|------|------|
| フロントエンド | HTML / CSS / JavaScript |
| バックエンド・DB | Supabase |
| 認証 | Supabase Auth |
| Supabase SDKバージョン | `@2.39.3`（安定版固定） |
| 開発環境 | VSCode + Live Server拡張 |
| 公開 | GitHub Pages |
| AIコーディング | Cowork（Claude） |
| PWA | manifest.json + メタタグ |

### Supabase接続情報
| 項目 | 値 |
|------|------|
| Project URL | `https://rkclfgpewmjdluwufjng.supabase.co` |
| anon key | コード内の `SUPABASE_ANON_KEY` 変数に記載 |
| service role key | コード内の `SUPABASE_SERVICE_KEY` 変数に記載（admin.htmlのみ） |

---

## 6. データベース設計

### user_profiles
| カラム | 型 | 内容 |
|---------|-----|------|
| id | uuid | Supabase AuthのユーザーIDと紐づく |
| employee_id | text | 社員番号 |
| name | text | 表示名 |
| role | text | member / manager / manager_admin / dept_admin |
| grade | int | 等級（0〜5） |
| store | text | 所属店舗 |
| area | text | 所属エリア |
| department_id | int8 | 所属部署ID |

### check_items
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| category_id | int8 | カテゴリID |
| name | text | 項目名 |
| grade | int | 対象等級 |
| sort_order | int | 並び順（null は末尾扱い） |

### categories
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| name | text | カテゴリ名 |
| department_id | int8 | 部署ID |
| sort_order | int | 並び順（null は末尾扱い） |

### test_sets
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| name | text | 問題集名 |
| category_id | int8 | カテゴリID |
| grade | int | 対象等級 |
| pass_score | int | 合格基準点 |

### test_questions
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| test_set_id | int8 | 問題集ID |
| question | text | 問題文 |
| type | text | choice / truefalse / text |
| point | int | 配点 |

### test_choices
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| question_id | int8 | 問題ID |
| body | text | 選択肢本文（○×は 'maru'/'batu'） |
| is_correct | bool | 正解かどうか |

### test_results
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| user_id | uuid | 受験者ID |
| category | text | カテゴリ名（記録用） |
| grade | int | 受験時の等級 |
| **test_set_id** | **int8** | **問題集ID（v20〜追加）** |
| score | int | 選択式の自動採点点数 |
| passed | bool | 合否（記述式あり→暫定 false 保存。合否表示は合計点で再計算） |
| **answers** | **jsonb** | **{question_id: {type, answer, correct}}（v20〜追加）** |
| taken_at | timestamptz | 受験日時 |

### text_answers
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| user_id | uuid | 受験者ID |
| category | text | カテゴリ名（記録用） |
| grade | int | 受験時の等級 |
| **test_set_id** | **int8** | **問題集ID（v20〜追加）** |
| **question_id** | **int8** | **問題ID（v20〜追加）** |
| question | text | 問題文（記録用） |
| answer | text | 記述回答 |
| score | int | 採点後の点数（null = 未採点） |
| graded_at | timestamptz | 採点日時 |
| submitted_at | timestamptz | 提出日時 |

### retest_permissions
| カラム | 型 | 内容 |
|---------|-----|------|
| id | int8 | 自動採番 |
| user_id | uuid | 対象メンバーID |
| **test_set_id** | **int8** | **問題集ID（v20〜追加）** |
| category | text | カテゴリ名 |
| grade | int | 等級 |
| granted_at | timestamptz | 許可日時 |
| used_at | timestamptz | 使用日時（null = 未使用） |

### その他テーブル（v19と同様）
- check_steps / check_progress / comments / departments / areas / stores / category_departments / grade_up_requests

### 合否の計算方法（v20〜）

```
合計点 = test_results.score（選択式・○×の自動採点）
         + SUM(text_answers.score)（その test_set_id の記述式採点済み合計）

合否 = 合計点 >= test_sets.pass_score

採点中 = text_answers に score=null の行が1件でもある場合
         → 合否未確定。表示上「採点中」とし、最終合否は採点完了後に再計算
```

---

## 7. v20 で追加・修正した主な機能

### テスト画面（test_page.html）— 全面刷新
1. **動的テスト読み込み**：`?set_id=N` で問題集IDを受け取り、DB から問題・選択肢を取得して描画
2. **3種類の問題タイプ**：4択（radio）/ ○×（ボタン）/ 記述（textarea）
3. **合否ロジック変更**：選択式点＋記述式採点済み合計 ≥ pass_score で合格
4. **採点中ステータス**：記述式ありテストは提出後「採点中」表示、合否は採点後に確定
5. **受験済み閲覧**：過去の回答・正誤・採点結果を表示（編集不可）
6. **二重送信ガード**：`SUBMITTED` フラグ ＋ DB の既存 test_results / text_answers 存在チェックで再送信を完全ブロック
7. **passed の保存方式**：記述式ありの場合は `false` を保存（NOT NULL 制約対応）。表示時は合計点で再計算

### メンバー画面（member_top.html）
1. **テスト選択モーダル**：テストボタン → そのカテゴリの問題集一覧モーダル → 問題集を選択 → test_page.html へ遷移
2. **ステータス表示**：未受験 / 採点中 / 合格X点 / 不合格X点
3. **採点中の受験ブロック**：ボタンラベルを「採点待ち」に変更。test_page.html 側でも二重ブロック
4. **全ステータスでテスト画面遷移可能**：合格・不合格でも結果閲覧のためにテスト画面に入れる

### 管理画面（admin.html）
1. **部署・エリア・店舗管理**：店舗の移動・削除ボタンを右寄せ表示
2. **問題集追加フロー**：部署選択 → その部署のカテゴリのみ絞り込み表示
3. **○×問題タイプ**：問題追加モーダルに○×タイプを追加。「○が正解」「×が正解」をラジオボタンで選択可能
4. **採点ページ**：問題の設定配点（test_questions.point）を取得し、入力欄の最大値・表示に使用
5. **再試験管理**：採点中（未採点の text_answers あり）は再試験対象から除外。合計点 < pass_score のみ対象

### メンバー詳細画面（member_detail.html）
1. **テスト履歴テーブル**：問題集名・合計点（選択式＋記述式合計）・採点中ステータスを表示
2. **プロフィールカードのテスト点数**：最新テストの合計点を表示（採点中は「－」）

### CSV テンプレート
1. **メンバー CSV**：エリア名列を削除（店舗から自動解決）。列順：名前・社員番号・パスワード・部署名・店舗名（任意）・等級・役割
2. **店舗 CSV**：列順を 部署名 → 店舗名 → エリア名 に変更（部署から area を自動解決）

---

## 8. 役割一覧
| role | 表示名 | 権限 |
|---|---|---|
| member | メンバー | 自分の学習画面のみ |
| manager | MG | 全部署のダッシュボード閲覧 |
| manager_admin | 全権管理者 | 全部署閲覧＋全管理機能＋社員番号変更・PW変更・部署/エリア名編集 |
| dept_admin | 部署管理者 | 自部署のみ閲覧・管理（メンバー名前編集可） |

※ `admin` ロールは廃止済み

---

## 9. ログイン後の遷移先

```javascript
if (role === 'member') → member_top.html
if (role === 'manager' || role === 'dept_admin') → manager_dashboard.html
if (role === 'manager_admin') → admin.html
```
